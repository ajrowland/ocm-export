{
  "name": "ocm-export",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "export": "bun --env-file=.env run src/export.ts",
    "transform": "bun --env-file=.env run src/transform.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "consola": "^3.2.3",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
